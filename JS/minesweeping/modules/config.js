let sizeX = 16;
let sizeY = 16;
let numTiles = sizeX * sizeY;
let tileSize = 32;
let padding = 0;
let sizeMultiplier = 1;
let numBombs = 48;
let startPosX = 100;
let startPosY = 100;

export {
  sizeX,
  sizeY,
  numTiles,
  tileSize,
  padding,
  sizeMultiplier,
  numBombs,
  startPosX,
  startPosY,
};
