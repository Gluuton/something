const trianglesPerSecond = 20;

const canvasWidth = 1280;
const canvasHeight = 720;

const tetrahedralCoords = [
  [1, 0, 2],
  [0, 1, 2],
  [1, 1, 2],
];

export { trianglesPerSecond, canvasHeight, canvasWidth };
